version: '3.8'
services:
  kanban:
    build:
      dockerfile: Dockerfile
    ports:
      - "17808:17808"
    environment:
      - DATA_PATH=/data/app.db
    volumes:
      - kanban_data:/data
volumes:
  kanban_data:
